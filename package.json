{
  "name": "meeting-room-booking-system",
  "version": "1.0.0",
  "description": "新竹市衛生局會議室預約管理系統 (Production Main Branch)",
  "main": "src/index.js",
  "scripts": {
    "dev": "npx wrangler dev",
    "build:css": "npx @tailwindcss/cli -i ./src/input.css -o ./public/vendor/tailwind.min.css --minify",
    "deploy": "npm run build:css && npx wrangler deploy --env \"\"",
    "deploy:prod": "npm run build:css && npx wrangler deploy --env \"\"",
    "deploy:dev": "npm run build:css && npx wrangler deploy --env development"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "wrangler": "^4.119.0"
  }
}
